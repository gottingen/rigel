cmake_minimum_required(VERSION 3.10)

project(googletest-external NONE)

include(ExternalProject)
ExternalProject_Add(googletest
  URL               "${rigel_gtest_download_url}"  # May be empty
  SOURCE_DIR        "${rigel_gtest_src_dir}"
  BINARY_DIR        "${rigel_gtest_build_dir}"
  CONFIGURE_COMMAND ""
  BUILD_COMMAND     ""
  INSTALL_COMMAND   ""
  TEST_COMMAND      ""
)
